{
  "buildCommand": "npm run build",
  "outputDirectory": ".vercel/output",
  "redirects": [
    {
      "source": "/:path((?!.well-known).*)*",
      "destination": "/:path*",
      "permanent": false
    }
  ]
}
